(library
  (name recipes)
  (inline_tests)
  (libraries
    core_kernel
    uri
    base64
  )
  (preprocess (pps
    ppx_jane
    ppx_blob
  ))
  (preprocessor_deps
    (glob_files "images/*.png")
  )
)
